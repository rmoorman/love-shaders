#!/bin/env python
import os
import subprocess
excluded_folders = ["old", "build", ".git"]

os.chdir("..")
dirs = filter(lambda x: not x in excluded_folders, os.walk(".").__next__()[1])

def build(directory):
    subprocess.call(["zip", directory+".zip", "*"], shell=True)

for directory in dirs:
    build(dir)

